"""Arquivo legado (obsoleto).

Esta migration foi gerada fora de `alembic/versions` e com `down_revision`
inválido (`88a6cb608e9b`, inexistente). O estado final já está consolidado em:
`alembic/versions/9804a6a833db_initial.py`.

Mantido apenas para histórico local até remoção manual do arquivo.
Não é carregado pelo Alembic.
"""
